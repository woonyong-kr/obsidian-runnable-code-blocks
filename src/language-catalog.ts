// Metadata only: safe for build tooling without importing editors or runners.
export { SUPPORTED_LANGUAGES, supportedLanguage } from "./supported-languages";
export { parseRunnableFence, fenceForLanguage } from "./contracts";
